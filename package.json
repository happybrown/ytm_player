{
  "name": "ytm_player",
  "version": "1.0.0",
  "description": "Youtube Music Player",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-installer-debian --src ytm_player-linux-x64/ --dest installers/ --arch amd64 --config config.json",
    "build": "electron-packager . ytm_player --overwrite --platform=linux --arch=x64 --icon=app_ico.png --prune=true"
  },
  "keywords": [
    "Electron",
    "Youtube Music"
  ],
  "author": "Brown",
  "license": "CC0-1.0",
  "devDependencies": {
    "asar": "^3.0.3",
    "electron": "^13.1.1",
    "electron-installer-debian": "^3.1.0",
    "electron-packager": "^15.2.0"
  }
}
